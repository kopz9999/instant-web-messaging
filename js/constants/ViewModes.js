const SPLIT = 'split';
const OVERLAY = 'overlay';
const MANUAL = 'manual';
const FULL_SCREEN = 'fullScreen';
const NOTIFICATIONS = 'notifications';

/**
 * @enum {String}
 */
const VIEW_MODES = {
  SPLIT, OVERLAY, MANUAL, FULL_SCREEN, NOTIFICATIONS
};

module.exports = VIEW_MODES;