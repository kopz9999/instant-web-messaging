const SPLIT = 'split';
const OVERLAY = 'overlay';
const MANUAL = 'manual';
const FULL_SCREEN = 'fullScreen';
const NOTIFICATIONS = 'notifications';
const MODAL = 'MODAL';

/**
 * @enum {String}
 */
const VIEW_MODES = {
  SPLIT, OVERLAY, MANUAL, FULL_SCREEN, NOTIFICATIONS, MODAL
};

module.exports = VIEW_MODES;