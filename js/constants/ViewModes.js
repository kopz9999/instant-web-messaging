const SPLIT = 'split';
const OVERLAY = 'overlay';
const MANUAL = 'manual';
const FULL_SCREEN = 'full_screen';

/**
 * @enum {String}
 */
const VIEW_MODES = {
  SPLIT, OVERLAY, MANUAL, FULL_SCREEN
};

module.exports = VIEW_MODES;